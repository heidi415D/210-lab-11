#include <iostream>
#include <string>
using namespace std;

// Album system
// Each album has a name, artist, and dynamic array of songs
struct Album {
    string name;   // album name
    string artist; // album artist
    string *songs; // dynamic array of songs
    int songCount;
};

// Function to create a new album
Album makeAlbum(string n, string c, int count){
    Album a;
    a.name = n;
    a.artist = c;
    a.songCount = count;
    a.songs = new string[count];

    // filling in songs
    for (int i = 0; i < count; i++){
        cout << "Enter song " << (i + 1) << " by " << c << ": ";
        getline(cin, a.songs[i]);
    }
    return a;
}

// function to print out album 
void printAlbum(Album &a){
    cout << "Album: " << a.name << " by " << a.artist << endl;
    for (int i = 0; i < a.songCount; i++){
        cout << "   " << a.songs[i] << endl;
    }
}

int main() {
    int howMany = 1; // test with one album
    Album *albums = new Album[howMany];

    albums[0] = makeAlbum("Study Mix", "Frank Ocean", 3);
    
    for (int i = 0; i < howMany; i++){
        printAlbum(albums[i]);
    }
}
